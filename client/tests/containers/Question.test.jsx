import Question from 'Containers/Question';

describe('Question', () => {
  it('should exist', () => {
    expect(Question).toBeDefined();
  });

  it('should render the question page');
  it('should load the question by ID from the API after page load');
  it('should load the answers by questoin ID from the API after page load');
});
