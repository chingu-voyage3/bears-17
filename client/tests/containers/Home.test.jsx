import Home from 'Containers/Home';

describe('Home', () => {
  it('should exist', () => {
    expect(Home).toBeDefined();
  });

  it('should render the home page');
  it('should load the questions from the API after page load');
});
